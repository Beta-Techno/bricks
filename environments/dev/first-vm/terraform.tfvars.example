# These values should come from the first-node outputs
api_url = "https://10.0.0.10:8006/api2/json"  # Change this to your Proxmox API URL

# The automation_user object should be populated with values from first-node output
# You can get these values by running:
# terraform output -raw automation_user
# Replace the placeholder values below with the actual values from first-node output
automation_user = {
  userid       = "automation@pam"      # This will be the actual user ID from first-node
  token_id     = "terraform-token"     # This will be the actual token ID from first-node
  token_secret = "your-token-secret"   # This will be the actual token secret from first-node
} 